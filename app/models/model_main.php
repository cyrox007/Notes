<?php
    class Model_Main extends Model {
        public function __construct() {
            /* $db = $this->connect_db("base.db"); // коннектимся к базе
            $res = $this->get_data($db, "users", "username", "admin2");
            while($row = $res->fetchArray(SQLITE3_ASSOC)) {
                var_dump($row);
                echo("<br>");
            }

            $db->close(); */
        }
    }